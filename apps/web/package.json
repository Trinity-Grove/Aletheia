{
  "name": "@aletheia/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --filter @aletheia/contracts build && pnpm --filter @aletheia/ui build",
    "build": "next build",
    "dev": "next dev",
    "lint": "eslint . --config ../../packages/eslint-config/index.mjs --ignore-pattern .next",
    "start": "next start",
    "pretest": "pnpm --filter @aletheia/contracts build && pnpm --filter @aletheia/ui build",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "pretypecheck": "pnpm --filter @aletheia/contracts build && pnpm --filter @aletheia/ui build",
    "typecheck": "next typegen && tsc --noEmit"
  },
  "dependencies": {
    "@aletheia/contracts": "workspace:*",
    "@aletheia/ui": "workspace:*",
    "next": "16.3.2",
    "qrcode": "^1.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^24.0.0",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.1.0",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
